{
  "name": "atlas-desktop-shell",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm --prefix ../web-ui run dev",
    "build": "npm --prefix ../web-ui run build",
    "preview": "npm --prefix ../web-ui run preview",
    "tauri:dev": "npm --prefix ../web-ui run tauri:dev",
    "tauri:build": "npm --prefix ../web-ui run tauri:build"
  }
}
